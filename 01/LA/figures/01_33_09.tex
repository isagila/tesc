\begin{tikzpicture}
  \begin{axis}[
      hide axis,
      colormap/cool,
      samples = 50,
      domain = 0 : 2 * pi,
      domain y = -5 : 5,
  ]
    \addplot3[surf] (
      {abs(y) * cos(deg(x))}, {abs(y) * sin(deg(x))}, {y}
    );
  \end{axis}
\end{tikzpicture}