\begin{tikzpicture}
  \begin{axis}[
      hide axis,
      colormap/cool,
      samples = 50,
      domain = 0 : 2 * pi,
      domain y = -5 : 5,
  ]
    \addplot3[surf] (
      {sqrt(1 + abs(y)) * cos(deg(x))}, {sqrt(1 + abs(y)) * sin(deg(x))}, {y}
    );
  \end{axis}
\end{tikzpicture}